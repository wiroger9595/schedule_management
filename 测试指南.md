# 完整測試指南

## 快速開始 (推薦)

### 1. 快速驗證 (2 分鐘)
驗證系統是否正常運作，包括 AI 調用和數據庫存儲：

```bash
cd /Users/chenrobert/Documents/code_life/schedule_management
source .venv/bin/activate
python validate_setup.py
```

**預期結果**:
```
✅ Testing with Groq (index 1)
[ 1/10] 創建簡單行程 ✅ Saved to DB
[ 2/10] 完整地點時間 ✅ Saved to DB
...
[ 10/10] 時間修改 ✅ Saved to DB
============================================================
✅ Validation Complete: 10/10 passed, 0 errors
📊 Results saved to ai_test_result table
```

---

## 完整測試方案

### 2. 運行 90 個完整測試 - 特定提供者

#### 使用 Groq (推薦，目前最穩定)
```bash
python optimize_ai_assistant.py --models 1 --report ai_test_report_groq.html
```
- **耗時**: 15-20 分鐘
- **通過率**: 預計 70-80%
- **輸出**: HTML 報告 + 數據庫存儲

#### 使用 HuggingFace (如果穩定)
```bash
python optimize_ai_assistant.py --models 0 --report ai_test_report_hf.html
```

#### 使用 Cerebras (備援)
```bash
python optimize_ai_assistant.py --models 2 --report ai_test_report_cerebras.html
```

#### 使用 Gemini (最後手段)
```bash
python optimize_ai_assistant.py --models 3 --report ai_test_report_gemini.html
```

---

### 3. 運行所有提供者的測試
```bash
python optimize_ai_assistant.py --report ai_test_report_all.html
```
- **耗時**: 1-2 小時（取決於 rate limit）
- **輸出**: 對比所有提供者的性能

---

## 檢查測試結果

### 方式 1: 查看數據庫統計
```bash
python check_db_results.py
```

**輸出範例**:
```
Total test results in DB: 100
Recent results:
  [parse_1] parsing: True (Groq/llama-3.3-70b)
  [parse_2] parsing: False (Groq/llama-3.3-70b)
  ...

📊 Results by category:
  parsing             :  18/ 20 ( 90.0%)
  intent              :  14/ 15 ( 93.3%)
  location            :  16/ 20 ( 80.0%)
  past_schedule       :  10/ 15 ( 66.7%)
  participants        :   8/ 12 ( 66.7%)
  edge_case           :   5/  8 ( 62.5%)
```

### 方式 2: 查看 HTML 報告
測試完成後，使用瀏覽器打開報告：
```bash
open ai_test_report_groq.html
```

報告包含：
- 整體通過率
- 按類別的詳細統計
- 常見錯誤分析
- 改進建議

---

## 單個測試案例驗證

### 方式 1: 簡單單元測試
```bash
python test_db_save.py
```

測試單個訊息並驗證數據庫存儲。

### 方式 2: 測試 HuggingFace 集成
```bash
python test_hf_simple.py
```

驗證 HuggingFace API 端點是否可用。

---

## 實時監控測試進度

### 查看正在運行的測試
```bash
ps aux | grep optimize_ai_assistant | grep -v grep
```

### 實時查看數據庫進度
```bash
# 每 5 秒檢查一次數據庫
while true; do
  clear
  python check_db_results.py
  sleep 5
done
```

---

## 分析和優化工作流

### 1. 運行驗證測試
```bash
python validate_setup.py
```

### 2. 如果驗證通過，運行完整測試
```bash
python optimize_ai_assistant.py --models 1 --report test_report.html
```

### 3. 查看結果並分析
```bash
python check_db_results.py
open test_report.html
```

### 4. 根據結果優化 Prompt
修改 `server/app/services/prompt_builder.py` 中的系統提示和規則

### 5. 重新測試驗證改進
```bash
# 清除舊結果
# sqlite3 ~/.../database.db "DELETE FROM ai_test_result WHERE category='parsing'"

# 重新測試
python optimize_ai_assistant.py --models 1 --report test_report_v2.html
```

---

## 常見問題排查

### ❓ 提示：找不到模組
```bash
source .venv/bin/activate  # 激活虛擬環境
```

### ❓ 數據庫連接失敗
檢查環境變數：
```bash
grep DATABASE_URL server/.env-stage
```

確保 `.env-stage` 存在並包含正確的數據庫信息。

### ❓ HuggingFace 報錯
系統會自動 fallback 到 Groq，不需要手動操作。

### ❓ Groq 被限速 (Rate Limited)
系統會自動重試和 backoff。如果仍然失敗，可以：
1. 稍後再試
2. 切換到其他提供者
3. 減少測試數量

---

## 推薦的測試計劃

### 第一次運行 (快速驗證)
```bash
# 1. 驗證系統是否正常 (2 分鐘)
python validate_setup.py

# 2. 查看結果
python check_db_results.py
```

### 第二次運行 (完整測試)
```bash
# 1. 運行 90 個完整測試 (15-20 分鐘)
python optimize_ai_assistant.py --models 1 --report ai_test_report_groq.html

# 2. 等待完成，監控進度
python check_db_results.py  # 每隔幾分鐘運行一次

# 3. 查看最終報告
open ai_test_report_groq.html
```

### 第三次運行 (優化後驗證)
修改 prompt 後重新運行：
```bash
# 重新運行相同的測試以比較結果
python optimize_ai_assistant.py --models 1 --report ai_test_report_groq_v2.html
```

---

## 測試環境檢查

### 驗證所有依賴
```bash
source .venv/bin/activate

# 檢查 Python 版本
python --version

# 檢查關鍵模組
python -c "import sqlmodel; import openai; from huggingface_hub import InferenceClient; print('✅ All dependencies OK')"

# 檢查環境變數
grep -E "GROQ_API_KEY|HUGGINGFACE_API_KEY|DATABASE_URL" server/.env-stage | head -3
```

### 數據庫連接測試
```bash
python -c "
import sys
sys.path.insert(0, 'server')
from app.db.database import engine
from sqlmodel import Session, select
from app.models.ai_test_result import AITestResult
with Session(engine) as s:
    count = len(s.exec(select(AITestResult)).all())
    print(f'✅ DB connected. Existing results: {count}')
"
```

---

## 完整工作流示例

```bash
#!/bin/bash
cd /Users/chenrobert/Documents/code_life/schedule_management
source .venv/bin/activate

echo "🚀 開始 AI 測試優化工作流"

# 步驟 1: 快速驗證
echo -e "\n1️⃣ 運行快速驗證 (10 個案例)..."
python validate_setup.py

# 步驟 2: 檢查驗證結果
echo -e "\n2️⃣ 驗證結果統計..."
python check_db_results.py

# 步驟 3: 運行完整測試
echo -e "\n3️⃣ 運行完整測試 (90 個案例，耗時 15-20 分鐘)..."
python optimize_ai_assistant.py --models 1 --report ai_test_report_groq.html

# 步驟 4: 查看結果
echo -e "\n4️⃣ 測試完成！開啟報告..."
open ai_test_report_groq.html

echo -e "\n✅ 工作流完成！"
```

保存為 `run_test.sh` 然後執行：
```bash
chmod +x run_test.sh
./run_test.sh
```

