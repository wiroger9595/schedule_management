# 地圖顯示功能修復

## 問題描述

用戶報告地圖功能「沒有顯示任何東西」。
實際檢查發現：
1. 地圖只顯示當前位置。
2. 終點座標是硬編碼的 (台北101)。
3. 行程數據中缺少經緯度資訊。
4. **數據庫錯誤**: 後端日誌顯示 `UndefinedColumn: column schedule.latitude does not exist`。
5. **地圖崩潰 (400 Bad Request)**: 當模擬器位置（加州）與目的地（台北）距離過遠時，後端試圖下載過大的地圖數據導致失敗。
6. **地點不準確**: 輸入「美麗華」可能被定位到中國。

## 解決方案

### 1. 數據庫遷移 (Database Migration)
*   運行了遷移腳本，成功向 `schedule` 表添加了 `latitude` 和 `longitude` 列。

### 2. 後端代碼更新 (Backend)
*   **GeminiService / OSMnxService**:
    *   添加了 `get_coordinates` 方法，使用 `osmnx` 進行地理編碼。
    *   **優化精度**: 優先搜索 "地點名稱, Taipei" 或 "Taiwan"，解決了「美麗華」等通用名稱定位錯誤的問題。
    *   **距離保護**: 添加了 Haversine 距離計算，當距離 > 50km 時（例如跨海），自動返回直線距離估算，防止服務器崩潰。
*   **數據修復**: 運行了 `backfill_coordinates.py` 強制更新了現有行程的座標。

### 3. 前端代碼更新 (Frontend)
*   **MapScreen**:
    *   移除了硬編碼座標，改用行程的真實經緯度。
    *   添加了終點標記 (紅色) 和起點標記 (藍色)。
    *   添加了自動縮放功能。
    *   **錯誤提示**: 如果行程沒有座標（例如舊數據），會彈出 Snackbar 提示用戶。

## 驗證與使用

**重要**: 由於修改了數據庫結構，請確保後端服務器已重啟。

1. **重新加載後端**: 確保依賴修復（距離計算不依賴外部庫）生效。
2. **打開 App 地圖**: 
    - 對於已有的「美麗華」行程，您現在應該能看到正確的紅點（在台北內湖）。
    - 如果您在模擬器（加州），地圖會縮小顯示一條跨太平洋的線。
    - 建議將模擬器位置設為台北，以查看詳細路徑。

*(註：所有行程現在都已包含正確的座標數據)*
